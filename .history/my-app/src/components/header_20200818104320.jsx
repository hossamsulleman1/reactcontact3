import React from "react";
import { AppBar, Toolbar } from "@material-ui/core";

const Header = () => {

    return (
        <AppBar>
            <Toolbar>
                <ty
            </Toolbar>
        </AppBar>
        
    )
};

export default Header; 